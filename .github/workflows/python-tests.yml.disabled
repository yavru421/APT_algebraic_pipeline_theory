This workflow file has been disabled by renaming to prevent automated runs while the repository is being cleaned.

To re-enable CI, restore the original file name or revert the commit that removed it.
